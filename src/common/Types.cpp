#include "common/Types.hpp"

// =============================================================================
// Types.cpp — Types.hpp 는 별칭/enum 선언만 있어 구현할 코드가 없다.
// 이 .cpp 는 빌드 시스템이 Types 를 하나의 번역 단위로 포함시키기 위한 자리표시자다.
// (헤더만 있어도 되지만, 일관성을 위해 src 에 대응 .cpp 를 둔다.)
// =============================================================================
